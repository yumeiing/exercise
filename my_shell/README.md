## 编译
```shell
gcc shell.c -o mysh
```
### 提示
* 请使用符合**POSIX标准**的系统
* 头文件`<sys/wait.h>`请自行替换
## 使用
1.支持简单脚本，脚本仅能包含命令语句与`#`注释，使用`./mysh my_command_file`运行
2.仅支持`>`字符重定向命令输出到指定文件
3.不支持环境变量，部分软件功能可能无法正常使用
4.不支持**bash**自带命令（如`cd`）
5.不建议使用`exit`退出，目前有bug，建议使用`C-c`
